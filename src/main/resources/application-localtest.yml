#服务请求地址
url:
  #  dmp: http://127.0.0.1:9805
  dmp: http://dev.huidawanan.cn/gateway/api-ibms-dmp
  monitor: http://dev.huidawanan.cn/gateway/api-ibms-monitor
  alarmUrl: http://dev.huidawanan.cn/gateway/api-ibms-alarm
  alarmWebSocket: ws://dev.huidawanan.cn/gateway/ws-ibms-alarm/webSocketServer

  iotProject: http://10.199.205.86:18858
  iotWebSocket: ws://10.199.205.86:18888/websocket

spring:
  #  redis
  redis:
    #    sdk
    primary:
      host: 10.178.28.172
      port: 6379
      timeout: 1000000
      database: 5
      password: 69Vk98Sc1CJtSCW
      jedis:
        pool:
          # 连接池最大连接数（使用负值表示没有限制）
          max-active: 16
          # 连接池中的最大空闲连接
          max-idle: 8
          # 连接池中的最小空闲连接
          min-idle: 0
          # 当连接池耗尽时，等待连接返回的最大时间，单位毫秒；-1表示永不超时
          max-wait: -1
    #      control
    secondary:
      host: 10.178.28.172
      port: 6379
      timeout: 1000000
      database: 0
      password: 69Vk98Sc1CJtSCW
      jedis:
        pool:
          # 连接池最大连接数（使用负值表示没有限制）
          max-active: 16
          # 连接池中的最大空闲连接
          max-idle: 8
          # 连接池中的最小空闲连接
          min-idle: 0
          # 当连接池耗尽时，等待连接返回的最大时间，单位毫秒；-1表示永不超时
          max-wait: -1

  # kafka
  kafka:
    sdk:
      bootstrap-servers: 10.178.28.172:9092
    alarm:
      bootstrap-servers: ${spring.kafka.sdk.bootstrap-servers}
    control:
      bootstrap-servers: ${spring.kafka.sdk.bootstrap-servers}

  # mysql
  datasource:
    url: jdbc:mysql://10.178.28.172:3308/huiyun_new?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&autoReconnect=true&failOverReadOnly=false

